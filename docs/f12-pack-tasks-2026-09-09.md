# F12 (2026-09-09, 15:30-17:50): Oli's handover pack, T1-T9

Migrations 073-082. Edge functions: upsert-contact-from-sales-nav v21, enrich-company-websites v12,
capture-and-classify-reply v20, load-voice-asset v1 (new). Lovable commit 5e406098, published.
Nothing sent, no phantom launched, nothing deleted. Every state change carries a migration_audit run id.

## T1 Rulings from interim_intel.jsonl (073)
- C295 Telia: Out of Scope, archived (out_of_scope), insurance fields set, ruling + evidence prepended to
  additional_notes; contacts P130, P160-163, P248 -> Not relevant, next_action = ruling, date 2027-09-03.
  Found, not in the ruling: Telia Norge / Telia Danmark / Telia Finland (C724/C755/C774) exist as separate
  rows. Grouped under 'Telia Company AB' and flagged needs_review; NOT archived. Oli to rule.
- C315 Otto: parent_group set with the "decides locally per company" note. UNITO / Universal / OTTO Austria /
  Quelle / Lascana / Ackermann did not exist as rows; created C1343-C1348 under the UNITO group, Ackermann
  carrying the "DO NOT SOURCE AS A SEPARATE SWISS RETAILER" note.
- C316 Conrad + C979 voelkner: parent_group 'Conrad Electronic SE via RE-INvent Retail GmbH ... MD Heiko
  Voigt'; getgoods C1349, digitalo C1350, SMDV C1351 created in the same group.
  Contradiction: the pack says Conrad is "contacted". It is ARCHIVED as promoted_to_monday, i.e. already in
  Monday. Any approach to voelkner/getgoods/digitalo is a second approach to a Monday account.
- New gate `group_sibling_engaged` in fn_evaluate_gates (initial_message + connection_request): refuses when
  any sibling in the same parent_group is in Monday / Contacted / Active Lead / Partner or has an engaged
  contact. Acceptance test 3: getgoods (C1349) is refused, reason names Conrad "in Monday".
  Pushback: null parent_group passes the gate (118 companies have a group, the rest are unknown, not
  known-independent). A group we have not mapped yet is a hole the gate cannot see.

## T2 source_list (074)
7 contacts created since 5 Sep relabelled 'P0 Sales Nav List' -> 'Lovable Master List' (Veimann, Rolando,
Koerdt, van Osch, Bourdin, Guian, Meddah), sn_lists array corrected. Audit of the rest: no other row created
after 5 Sep carries the old label. Nothing deleted.

## T3 Identity and URLs (075, ingest v21)
- contacts.linkedin_url is ONLY a public /in/<slug> URL; contacts.linkedin_sales_nav_url is ONLY a
  /sales/lead/ URL. Two check constraints enforce it. 20 rows had a Sales Nav URL sitting in linkedin_url:
  moved, and linkedin_url rebuilt from the slug (null where there was none).
- contacts.url_provenance jsonb per field: workbook / watcher / manual / inferred, with basis and at.
  Backfilled for every row. Manual edits in the app now write source 'manual'; the watcher writes 'watcher'.
- Ingest v21 root cause of the blank degrees: the Sales Nav phantom sends connectionDegree, the old EF used
  it only for connection_status and never wrote connection_level. v21 writes it, dedupes on either URL,
  fills blank URL fields on re-import, and returns 422 connection_degree_missing (fail closed) when a
  non-1st-degree lead arrives without a degree.
- Backfill: 34 Accepted / Already connected rows set to '1st degree' (a connection is by definition 1st).
  Trigger fn_contacts_degree_guard auto-fills 1st degree for those statuses and refuses a 'Request sent'
  row with a null level on insert or status/level change.
  Pack said 84 blank-degree accepted/requested; live was 86 (87 with an archived row).
- Unfillable: 269 live contacts still have no degree (Not connected 148, Withdrawn 68, Request sent 53).
  166 have a URL, 103 have no URL at all. List: docs/f12/degree_backfill_phantom_2026-09-09.csv for
  Brad's phantom run tomorrow. The 53 Request-sent rows are grandfathered: the trigger fires only on a
  state change, so unrelated edits to those rows still save.

## T4 Cleanup: websites, countries, Sales Nav URLs (076, 081, enrich v12)
- companies.field_provenance jsonb { field: { source: stated|inferred|workbook, basis, at } }; helper
  fn_set_field_provenance. Inferred renders visibly distinct in the app (dashed, muted, chip with basis).
- Inferable from data already in Lovable: 1 website (a contact's email domain) and 3 countries (TLD or
  contacts' country). Marked inferred. Stated values were never overwritten.
- Remaining: 346 companies without website, 128 without country. These are workbook name-only stubs
  (mostly excel_v09). The existing enrich-company-websites EF (Apify Google search, about USD 0.0025 a query,
  60 per run) can fill websites and now stamps them inferred; NOT run, because it spends money. Brad's call.
- Inferred country NEVER unlocks email: the Email channel in fn_evaluate_gates uses only a stated country
  (contact country, else company country only when not inferred) and refuses 'country_unknown' otherwise.
  Acceptance test 4: DE cold email refused; Fixit (inferred country) refused country_unknown.
- Sales Nav URLs are never derivable (F50). View v_sourcing_queue_missing_sn_url = 218 rows, surfaced as
  the Reconciliation "Sourcing queue" tab with a paste-and-save input, count on Today.
  Pack said 168 missing; live is 253, 218 after excluding archived / not relevant.
- Pack counts vs live before inference: 345/128 in the pack, 347/131 live.

## T5 Estimates vs stated figures (077)
Design chosen: an append-only company_size table (rung E0-E5, devices_per_month, confidence, tier_mark,
basis, evidence_verbatim, stated_by, is_estimate, recorded_at/by). Constraint company_size_stated_or_estimate:
is_estimate=false requires rung E1 + evidence_verbatim + stated_by; an estimate can be anything but E1.
Trigger refuses update/delete (history is the audit). View v_company_size_current = latest row per company.
companies.annual_devices_sold now needs annual_devices_sold_evidence + annual_devices_sold_stated_by
(trigger fn_companies_stated_figure_guard). Acceptance test 6: an estimate into annual_devices_sold is refused.
No rows loaded yet: the pack's volume CSVs are estimates and belong in company_size as is_estimate=true,
which Oli should confirm before I bulk-load them (0 rows today).

## T6 voice_assets (078, 082, load-voice-asset EF)
Table voice_assets (id, team_id, layer 1-4, applies_to text[], body, version, updated_at, updated_by) plus
voice_asset_versions history. Trigger: a body change without a new version is refused; every insert or
version change is recorded (082 split the trigger into BEFORE guard + AFTER history; the first cut wrote the
history row before the parent existed and the FK refused the load).
Loaded from the pack (5 rows, 5 version rows):
  pier_rules          L1  all                                              v2.0 (24 Jul 2026)      81,475 chars
  pier_terminology    L2  all                                              v10.11 (3 Sep 2026)     61,041
  linkedin_architect  L3  connection_request_note, first_message_after_cr, dm_chaser, inmail_chaser  v1.7   21,113
  email_architect     L3  cold_email_open, warm_email_reply, email_chaser  v1.7 (24 Jul 2026)      30,683
  voice_oliver        L4  first_message_after_cr, warm_email_reply ONLY    pack copy of 9 Sep 2026 40,361
The ONE update path (manifest s4): POST load-voice-asset { id, layer, applies_to, version, body }.
The drafter is NOT rewired. It still reads pier_ea_documents.

Cutover plan (proposal, not done):
1. Drafter v33 reads voice_assets instead of pier_ea_documents: L1 + L2 always; L3 chosen by the
   message type (touch_type + channel -> applies_to); L4 only when applies_to contains the message type,
   which today means first_message_after_cr and warm_email_reply and nothing else. Same cache_control
   breakpoint on the static prefix, so the caching saving survives.
2. Stamping: every draft row gets outreach_log.voice_stack_versions = {"pier_rules":"v2.0 (24 Jul 2026)",
   "pier_terminology":"v10.11 ...", "linkedin_architect":"v1.7 ...", "voice_oliver":"..."} written in the same
   update that writes message_body. ai-edit-draft copies the stamp forward on an edit; a regenerate
   restamps. Null = drafted before the cutover. A draft can then be replayed against the exact text in
   voice_asset_versions.
3. Dry-run the same 5 contacts with v32 (EA docs) and v33 (voice_assets) and diff; cut over only when
   Oli signs the diff.
4. Retire the five overlapping pier_ea_documents rows after two clean days; keep the rest (response bank,
   customer journey) until they too move.
5. Version discipline: Oli edits the canonical file, Brad or I load it with a new version string; the
   trigger makes a silent edit impossible.

## T7 Classifier confidence (079, classifier v20)
outreach_log.reply_confidence (0-100) and reply_reasoning are persisted on every classified reply.
Elevation to 'In conversation' now requires Positive interest / Booked meeting AND confidence >= 70
(ELEVATE_MIN_CONFIDENCE); below that the reply is filed, the chase stops, the status waits for Oli and the
log shows elevation_withheld_low_confidence. Uncategorised is capped at 49 so it can never elevate.
70 is a proposal: the model's own "confident" band. UI shows NN%, the reasoning, and a "low confidence,
check" chip under 70. The 56 existing replies carry no confidence (classified before v20); no backfill,
re-classifying would spend money and rewrite Oli's notes.

## T9 Realtime + degree leftovers (080)
Publication supabase_realtime now includes outreach_log and contacts. App hook useRealtimeRefresh
subscribes on the Outreach page (all tabs), the touch popout and Today, debounced 500 ms, invalidates
outreach / today / contacts.
Verified live in Chrome on the published build: an UPDATE on an outreach_log row and then on a contacts row
each produced one round of refetches (list, counts, refusals, momentum) with no interaction; an 8 s control
window with no change produced zero requests. So: a Send flips Approved -> Sent, a watcher-filed reply
lands on Today, a status or owner change on a contact refreshes the cards, all without refresh. What does
NOT update live: companies (not in the publication), refusals and audit_log (not subscribed).

## Acceptance tests (Oli, section 11)
1 idempotent replay, 2 dedupe on either URL: unchanged from F8, re-checked by reading v21.
3 group guard, 4 email legality on stated country only, 6 estimate refused: verified by SQL above.
5 two URL fields never merged, 7 provenance visible, 8 sourcing queue, 9 confidence visible: shipped in the
app; the click-through is Brad's (the app is password-gated; I only observed the Realtime traffic).
10 nothing sent: send_status counts unchanged through the session.

## State snapshot (17:45)
live contacts 681 | no degree 269 (Request sent 53) | sourcing queue 218 | no URL at all 115
companies no website 346 (1 inferred) | no country 128 (44 inferred, incl. earlier runs) | with parent_group 118
pending_review 59 | replies 56 (0 with confidence yet) | voice_assets 5 | company_size 0
