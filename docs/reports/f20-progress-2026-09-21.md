# F20 progress — 2026-09-21 (evening). Evidence type stated per item, per Rule 2.

Nothing was sent, approved or dispatched from this session. No Make scenario or PhantomBuster agent edited.

## URGENT: Oliver's new leads are not arriving — database half of the trace (Make/PhantomBuster half in
docs/reports/f20-urgent-sales-nav-ingest-trace-2026-09-21.md when the read-only agent reports)
EVIDENCE: SELECTs on contacts, audit_log and automation_heartbeat.
- Newest contact: 2026-09-16 08:01:55 UTC. Contacts created per day: 15 Sep 116, 16 Sep 1, nothing since.
- (c) PROVEN, not assumed: the 20 Sep 08:01 run touched exactly THREE EXISTING contacts, Julia Koellges P860,
  Matthias Lorenz P857 and Erdinc Kanal P785 (all created 15 Sep), and the only fields that changed were
  sn_lists and updated_at. Updates, not inserts.
- The ingest function upsert-contact-from-sales-nav was LAST CALLED 2026-09-20 08:01:43 UTC (heartbeat
  last_seen_at, last_payload_rows 1). Nothing has reached Supabase since, so nothing Oliver added today was
  ever offered to the Lake. The break is upstream of Supabase: in what PhantomBuster exports, or in what Make
  decides is new. There is no dedupe inside the Lake discarding them, because the Lake was never asked.
- Known from F8 memory and worth checking first: the Sales Nav export phantom processes a bounded number of
  profiles per launch and skips ones it has already exported.

## F20.14 Impossible state combinations
EVIDENCE: SELECT on outreach_log + audit_log. Nothing repaired.
(a) The 2 Sent + rejected rows are NOT rejected drafts that were sent. Both are messages OLIVER SENT BY HAND IN
MAY, migrated from the workbook (no run id, observed_or_inferred migrated_unverified), and clicked Reject in
the UI in August while they wrongly sat in a review queue:
| Ref | Person | Company | Sent | Channel | Type | Rejected in UI | Reason given |
| P260 | Valentin Klein di Giacomo | Easy Cash | 15 May 2026 | LinkedIn DM | Introduction (T213) | 26 Aug, after being Approved the same day | "Voice off" |
| P088 | Christian Kollesch | MediaMarkt Saturn | 5 May 2026 | LinkedIn CR | Connection request (T098) | 25 Aug | "not a message, just a note" |
A real message went out in both cases, MONTHS BEFORE the reject click. The sequence is sent-then-rejected,
never rejected-then-sent. Not the serious case.
(b) Sent + superseded, 136 rows: **LEGITIMATE SENDS WITH A WRONG DRAFT FLAG.** 125 connection requests and 11
initial messages, 130 migrated, none with a run id, and **NONE has a sent twin** on the same contact, channel
and date. 128 of them were bulk-marked superseded on 26 Aug. They are real history.
Cancelled + sent, 109 rows: MY OWN BOOKKEEPING and legitimate: 46 synthetic CRs (migration 107), 61 empty
listing rows merged into keepers (112), 2 others. send_status is what every reader uses; draft_status was
left as it was so the merge stays reversible.
(c) NOT APPLIED. See F20.0(d): the constraint has to wait for the repair decision below.

## F20.0 The unshipped backlog
(a) pg_cron secrets — DONE. EVIDENCE: schema_migrations row `125_f20_0a_cron_bearer_from_vault`; SELECT shows
0 of 6 jobs carry a literal, 4 read vault.decrypted_secrets, every rewritten command parses (EXPLAIN), the
Vault value's md5 equals the old literal's md5; and a live authenticated call using the Vault value returned
HTTP 200 (`not_sendable`, the harmless probe). The value was copied inside SQL and never passed through this
session. My own send-queue-drain job read the bearer out of the chase-engine command, so it was switched too,
otherwise it would have broken. NOT YET SEEN: each job's next scheduled run returning 200 (chase engine 06:15
tomorrow, daily insight 08:00 weekday, enrichment Sunday). Rotation is now one vault.update_secret call.
(b) Realtime — DONE. EVIDENCE: schema_migrations row `126_…`; pg_publication_tables to be re-read in the
verification block below.
(c) i053 duplicate touch fix — ALREADY APPLIED on 18 Sep (migration 112, 62 empty rows merged, 124 log rows in
touch_merge_log). What remains open: 4 identical-text doubles and 6 same-day pairs with different text that
need a human, and the importer still has no shared key across its 11 id namespaces.
(d) THE 138 ROWS — **NOT APPLIED, AND MY OWN EARLIER ADVICE WAS WRONG.** In F17 I wrote that the UI tile was
right and these rows should move to Cancelled. Measured today: 136 Sent+superseded rows with NO sent twin.
Cancelling them would delete 125 real connection requests and 11 real messages from the history that
cooldown, duplicate-approach and sequence position all read. The correct repair is the OPPOSITE: set their
draft_status back to `sent`. That also means **the Sent tile has been undercounting by 136 for weeks**, which
reverses my F17 B1 conclusion. Brad's call before anything is written.
(e) Thread selects — SUPERSEDED BY WHAT SHIPPED. The Lovable audit on 21 Sep showed those selects are
display-only (no thread text is sent to any Edge Function) and the editor already filters to Sent rows; the
"unsent" marker shipped in Lovable 8ed5393a. Nothing further to fire.
(f) Still written-but-not-applied elsewhere: F17.1 rotate INTERNAL_APP_SECRET (Brad); generate-daily-insight
v17 deploy (Brad); F17.4 hard refusal of an unattributed Accepted; the 120 stamp trigger re-stamping an
'Already connected' relabel; F19.2 PhantomBuster webhook repointing (Brad, in the PhantomBuster UI).

## F20.11 Capacity dry run — EVIDENCE: SELECT over the four candidate functions with the gate evaluated
55 distinct contacts have ever been refused for research: confirmed. With the gate open the routes unblock
**36 contacts: 25 first messages, 7 chasers, 4 replies.** Cold InMail is unaffected (its candidate function
already requires deep research; 100 pass today). 36 is below 55, so no STOP.
**The finding that matters: 14 first-message candidates ALREADY passed every gate and were never drafted,
because team_settings.first_message_after_cr_enabled was false.** The gate was the second lock, not the first.
(c) Ceiling: team_settings.max_drafts_per_run = 20, enforced in chase-engine across all routes with 5 slots
reserved for replies. (e) Expected extra model calls: at most 20 a day while the backlog drains (about 50
contacts, so three mornings), then a handful. At £0.01-0.03 warm, under £1 a day.
(d) Logged as an improvement item (below).

## F20.1 The research gate warns
EVIDENCE: schema_migrations rows 127, 128, 129; information_schema rows for team_settings.research_gate_warns_only,
team_settings.max_drafts_per_run and outreach_log.research_warning; consent proof in migration_audit.
(a) ONE condition changed in fn_evaluate_gates, by exact text replacement of the live definition.
(c) **CONSENT PROOF: 1,598 evaluations before and after (all 799 active contacts x initial_message and
chaser). 505 outcomes changed; ALL 505 were company_not_deep_researched (501 now pass, 4 now
thread_text_missing). ZERO changes from any other code.** promise_of_quiet 34 = 34, dnc_or_opted_out 132 = 132,
contact_parked 134 = 134, group_sibling_engaged 58 = 58, pending_ruling 82 = 82, contact_replied 13 = 13,
allowance_exhausted 25 = 25. Two in-migration attempts at this proof timed out and rolled back cleanly; it
was then run in batches around a one-statement migration.
(b) The drafter now stamps EVERY draft for an un-researched company and fills outreach_log.research_warning.
Deploy result below. The red flag on the card is a Lovable change, NOT yet fired.
(d) First-message route switched on (migration 129). It first runs at 06:15 UTC tomorrow: expect up to 5
first messages, inside the ceiling of 20. NOT YET PRODUCED: I did not trigger the engine by hand.
(e) **WARNING FOR BRAD TO PASS TO OLIVER: a draft written for a company with no research is a thin draft.**
From tomorrow he will see more drafts and the new ones will be weaker: generic hooks, no insurer, no
programme detail. Each is marked. He agreed the trade; he should hear it before he sees it.
(f) The four reply candidates (Popescu, Koldby, Köhler, Thakooree) will now draft at 06:15, each flagged.

## F20.15 The InMail refusal and the first good send — EVIDENCE: SELECTs
(g) **Koellges P860: every downstream effect is correct.** Row Sent/sent, touch_date 21 Sep, sent_at
13:42:36, sent_body frozen (638 chars), audit send_completed + send_effects; contact Contacted,
awaiting_reply, chase_last_outbound_at 21 Sep, next due 28 Sep, chaser_count 0. One flag: her stored degree
still reads 2nd though a DM was delivered; the degree field is stale, as suspected for the 17.
Haimb P575: verified as Brad described. Charge to 127 at 13:47:27, refund to 128 at 13:48:02, row Cancelled,
send_error "finished". Not touched. (f) Never two open at once: the 06:16 draft was superseded by a manual
Regenerate at 13:43:53 and its replacement created 13:44:05; trg_one_open_draft is enabled.
(a)(b)(c)(d)(e) NOT STARTED.

## Deploy results and verification block
15:40 UTC: BOTH background agents (the deploy and the Make/PhantomBuster trace) died on a session rate limit
before producing anything. State re-measured, not assumed:
- Migrations 125-129: ALL APPLIED (schema_migrations latest is 129; team_settings reads research_gate_warns_only
  true, max_drafts_per_run 20, first_message_after_cr_enabled true; outreach_log.research_warning exists;
  pg_publication_tables lists improvements, improvement_comments, improvement_activity).
- Functions: **NOT deployed.** generate-draft-from-context is still v42 (ezbr d3398a95…) and chase-engine still
  v12 (ezbr dcdb27c8…). Source is committed (300c832). Both agents relaunched.
- WHY THIS MATTERS UNTIL THE DEPLOY LANDS: the gate is already open, so a draft generated now for an
  un-researched company carries NO warning (replies excepted), and the 06:15 UTC run would have no 20-draft
  ceiling. No draft has been created since the gate opened (0 rows since 18:00 BST), so nothing unflagged
  exists yet. If the deploy cannot land tonight, set team_settings.research_gate_warns_only to false.
- Oliver has sent 5 messages through the new send path since 14:00 UTC.
(further results appended as the agents report)
