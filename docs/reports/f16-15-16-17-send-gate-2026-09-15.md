# F16.15 / F16.16 / F16.17 report (2026-09-15)

## F16.15 The send path now checks the gates (send-approved-draft deployed as version 14; a version 13 was deployed by someone else in between)
- (a) fn_evaluate_gates runs immediately before the PhantomBuster launch, after the capacity checks, with the row's real channel and p_requested derived from touch_type (Connection request -> connection_request; Chase / Chaser N -> chaser; Follow up -> reply; else initial_message). A refusal aborts the launch, inserts a refusals row (context.source = send-approved-draft, stage = send), and returns { status: "refused", reason_code, reason_human } for the UI. If the gate call itself errors, nothing launches (HTTP 500 gate_evaluation_failed).
- (b) NO_SEND now includes 'Opted out' and 'Parked' as the backstop.
- Proof without sending: the function gained `dry_run: true`, which runs every check and returns would_launch or the refusal without launching. I could not call it (INTERNAL_APP_SECRET is not in this shell); the code path is asserted from source. Today there are 0 approved drafts at send_status Draft or Ready, and 0 sendable drafts at Opted out or Parked, so nothing is at risk this minute.
- Sweep: 159 sent rows belong to contacts who are NOW at Opted out, Parked, promise_of_quiet or DNC. Zero of them carry a phantom_run_id (none dispatched by this system) and zero have an audit_log status transition dated before the send: the statuses were set afterwards, the bulk on the 4 Sep migration. Latest such send is 2 Sep, pre go-live. Not a STOP. Full list is in the query output retained in the session; names on request.

## F16.16 Replies are not refused on group grounds (migration 105, drafter version 38)
- fn_evaluate_gates skips the group block when p_requested = 'reply'. initial_message, connection_request and chaser keep the absolute refusal; promise_of_quiet, dnc_or_opted_out, contact_parked and pending_ruling still refuse replies.
- Drafter: a reply for a company with an engaged sibling carries "GROUP COLLISION: linked to <sibling> (in Monday; linked by ...)" in the narrative and first guardrail, and group_note in the response.
- Popescu now evaluates to company_not_deep_researched on 'reply' (flag off), like the other five.
- Correction to the F16.3 report: with reply_ignores_research_gate ON, the six evaluate as follows from the gate order (research gate skipped, then thread_text_missing): Köhler, Thakooree, Stiemert, Moeller and Popescu pass; Koldby has one sent touch with no recorded text and would meet thread_text_missing. That is my reading of the gate; if Brad's test showed Koldby passing, the touch must have text now and I am wrong.

## F16.17 Migration numbering
- schema_migrations holds both 103_f16_1_duplicates_not_siblings_and_out_of_scope (11:32) and 103_f16_1_duplicates_route_to_review (11:35). Nothing re-applied.
- The repo now carries both names: Cowork's file records fn_company_duplicate_candidates exactly as in pg_proc; mine records fn_group_siblings_engaged exactly as in pg_proc plus the company_alerts changes. Both bodies were copied from pg_get_functiondef today.
- `supabase migration list` could not be run: the CLI on this machine is not logged in. The comparison was made against supabase_migrations.schema_migrations directly.
