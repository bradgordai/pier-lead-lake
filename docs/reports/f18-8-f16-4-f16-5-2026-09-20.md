# F18.8: F16.4 (company_archived reason) and F16.5 (cr_accepted_at), 2026-09-20

Project `qzfrcfzeiagziqjnfarw`. Audit run_id `f18-2026-09-20`. Nothing sent, no Edge Function called or deployed, no git commit.

Migrations applied: `119_f16_4_company_archived_reason`, `120_f16_5_cr_accepted_at` (files in `/migrations`, applied with the file's exact content).

Incident: the first apply of 119 failed with `duplicate key ... schema_migrations_pkey (version)=(20260920163710)`, a ledger timestamp collision with another session's `118b_f18_5_improvement_log_import_p06`. Verified nothing landed (CHECK unchanged, 6 rows still dnc_or_opted_out, 0 audit rows), halted, re-verified and re-applied on the coordinator's instruction. Second apply succeeded.

## F16.4

### 1. The 6 `dnc_or_opted_out` refusals (before)

| ref | contact | company (archived_at, archive_reason) | channel / requested | created_at | contact status |
|---|---|---|---|---|---|
| P271 | Adrien Serres | Recommerce Group (2026-09-04, promoted_to_monday) | LinkedIn DM / manual_regenerate | 2026-09-08 11:16:10 | In conversation |
| P271 | Adrien Serres | same | LinkedIn DM / manual_regenerate | 2026-09-08 11:16:31 | In conversation |
| P271 | Adrien Serres | same | LinkedIn DM / manual_regenerate | 2026-09-08 13:56:17 | In conversation |
| P046 | Kilian Kaminski | (re)furbed (2026-09-04, promoted_to_monday) | LinkedIn inMail / inmail_cold | 2026-09-15 05:04:02 | Contacted |
| P010 | Patrick Höijer | Foxway (2026-07-28, promoted_to_monday) | LinkedIn inMail / inmail_cold | 2026-09-15 05:04:02 | Cooldown |
| P047 | Peter Windischhofer | (re)furbed (2026-09-04, promoted_to_monday) | LinkedIn inMail / inmail_cold | 2026-09-15 05:04:03 | Contacted |

All 6: reason_human "<company> is archived, so this contact is out of scope.", context `{company_archived_at: ...}`. None of the 4 contacts has do_not_contact, promise_of_quiet, or an Opted out / Do not contact status. Genuine opt-outs: 0. `refusals` has no source column; the context shape and sentence identify the writer as the archive block of `generate-draft-from-context`.

### 2. Writers

- `fn_evaluate_gates` has NO archived-company branch. It returns `dnc_or_opted_out` only for (a) contact not found, (b) do_not_contact or outreach_status in ('Do not contact','Opted out','Not relevant','Left company'). Cross-check: the gate returns dnc_or_opted_out for 67 contacts (33 on live companies, 34 on archived companies) and that equals exactly the contacts matching branch (b) (33 / 34). So the archived-company cases come from contact status, not from an archive test. The brief assumed an archive branch in the function; there is none, so the function was NOT changed. md5(prosrc) before and after: `3cd182fa6deb99112aff3c5aaaf0f5ba`.
- Only writer for an archive: `supabase/functions/generate-draft-from-context/index.ts`, the `if (company?.archived_at)` block.
- Registry of codes: only the CHECK constraint `refusals_reason_code_check` (no enum, no lookup/label table, no other function or view mentions the code). Extended with `company_archived`; the label "Company archived" is recorded in the constraint comment.

Observation (not changed): "Not relevant" and "Left company" also map to `dnc_or_opted_out` in the gate, so any future refusal for those would again inflate a consent metric. No such refusal rows exist today.

### 3. Migration 119

CHECK extended (12 existing codes + `company_archived`); 6 rows re-coded `dnc_or_opted_out -> company_archived`, each logged in migration_audit (phase `f16_4_company_archived_reason`, action `reason_code_recoded`, before/after + reason_human/context/channel/requested). reason_human left as written (already accurate). Gate order untouched.

### 4. Local Edge Function edit, AWAITING DEPLOY (deploy only after 119, which is done)

`supabase/functions/generate-draft-from-context/index.ts`, archive block (about lines 489-506):
- comment lines 490-491 rewritten (F16.4 note)
- line 495: refusals insert `reason_code: "dnc_or_opted_out"` -> `"company_archived"`
- line 502: log `reason_code` -> `"company_archived"`
- line 503: returned `reason_code` -> `"company_archived"`

Line 410 (`gate.reason_code === "dnc_or_opted_out" || ... "promise_of_quiet"` raising a reconciliation note) is deliberately unchanged. Any UI that switches on the returned reason_code (Lovable) should learn `company_archived`; it previously received `dnc_or_opted_out` for this case.

### 5. Consent proof

fn_evaluate_gates run for all 811 contacts x ('LinkedIn DM','initial_message') and ('LinkedIn inMail','chaser') = 1,622 evaluations, in 6 chunks ordered by contact id (statement timeout), hashing contact id + combo + reason_code + reason_human + context per row.

| chunk | before md5 | after md5 |
|---|---|---|
| 1-140 | c89c7280859c1fda06de09dea2c9658f | c89c7280859c1fda06de09dea2c9658f |
| 141-280 | 07e035f3fb85c9730b23cd0ca89badb6 | 07e035f3fb85c9730b23cd0ca89badb6 |
| 281-420 | 45f78ec8ac40518512160f19f71bd1b7 | 45f78ec8ac40518512160f19f71bd1b7 |
| 421-560 | 5b7a9e89ba0bf4239a073e3bab6c9140 | 5b7a9e89ba0bf4239a073e3bab6c9140 |
| 561-700 | 72f03b75030433798e5dd365d7b408cd | 72f03b75030433798e5dd365d7b408cd |
| 701-811 | 194bad7fc79afb223b72c72a7bbb9e2f | 194bad7fc79afb223b72c72a7bbb9e2f |

Per-contact results identical in every chunk: zero differences (stricter than the permitted difference). Gate output `dnc_or_opted_out`: 134 before, 134 after (67 contacts x 2); `company_archived` from the gate: 0 before, 0 after.

Before distribution, non-archived companies (DM initial / inMail chaser): pass 277 / 243; company_not_deep_researched 222 / 209; contact_parked 45 / 45; dnc_or_opted_out 33 / 33; pending_ruling 29 / 29; group_sibling_engaged 27 / 27; promise_of_quiet 15 / 15; allowance_exhausted 0 / 23; thread_text_missing 0 / 18; contact_replied 0 / 6. Archived companies: pass 46 / 34; company_not_deep_researched 42 / 42; dnc_or_opted_out 34 / 34; contact_parked 24 / 24; pending_ruling 12 / 12; group_sibling_engaged 3 / 3; promise_of_quiet 2 / 2; contact_replied 0 / 7; thread_text_missing 0 / 3; allowance_exhausted 0 / 2.

### 6. pending_ruling refusals citing an archive (report only, not re-coded)

45 pending_ruling refusals in total; 17 cite an archive:
- 11 Conrad Electronic contacts (P811, P812, P814, P816, P818, P819, P820, P823, P825, P828, P829): "Conrad is archived promoted_to_monday (live Monday deal); getgoods/digitalo/Voelkner share the group". Company IS archived.
- 1 Swappie (P114): "Swappie is archived promoted_to_monday". Company IS archived.
- 1 getgoods.com (P884): same Conrad group sentence; its own company is NOT archived.
- 4 rows on re/commerce Group (P862 x1, P880 x3): "may be the archived Recommerce Group (C149); Oliver to decide". Company NOT archived.
So 12 rows sit on archived companies, 5 on live companies that only reference an archived sibling.
Side finding: two P880 rows (2026-09-18, written by the drafter from the gate) read "Awaiting Oliver's ruling: Awaiting Oliver's ruling: ..." because the gate re-reads the newest pending_ruling refusal and prefixes it again; the prefix will keep stacking on each refusal.

### 7. Result

`select count(*) from refusals where reason_code='dnc_or_opted_out'` = **0**. `company_archived` = 6. No survivors.

## F16.5

### 1. Column + backfill (migration 120)

`contacts.cr_accepted_at timestamptz`, nullable, no default.

Literals verified first: audit_log.entity_type 'contacts'; sources 'manual' (6,388) and 'system_backfill_f17' (812); connection_status enum contains 'Accepted'.

Transitions to Accepted (before IS DISTINCT FROM 'Accepted', source <> system_backfill_f17): 119 rows on 118 contacts.

| count | value |
|---|---|
| Genuine (non-2026-09-04) transitions found | **42** (expected "about 33"). 4 are `created` rows (25-28 Aug) whose contacts no longer exist, leaving 38 transitions on 37 live contacts: 24 from 'Already connected', 11 from 'Not connected', 3 from 'Request sent' |
| Contacts stamped | **36** (earliest transition each, still 'Accepted' today): 24 from Already connected, 11 from Not connected, 1 from Request sent. Range 2026-08-25 07:04 to 2026-09-18 19:04 UTC. Not stamped: Moona Pohjola (2 transitions on 2026-07-30, no longer Accepted) |
| Transitions deliberately NOT stamped (dated 2026-09-04) | **77** (43 created, 18 Not connected, 12 Request sent, 4 Already connected), 76 of those contacts still Accepted. 0 stamps carry the date 2026-09-04 |
| Currently Accepted with cr_accepted_at NULL ("date unknown") | **128** of 164 |

The 2026-09-04 exclusion was applied on both the UTC and the Europe/London date; none of the stamped contacts also has a migration-day transition, and none had earlier Accepted evidence in audit_log.

Judgement call to review: 24 of the 36 stamps come from 'Already connected' -> 'Accepted'. They fit the brief's predicate and all were written by the connection watcher cron (xx:04, no actor, last_contacted changed) before F15.4 began treating 'Already connected' as already accepted, i.e. the Recently Connected phantom did report them. If they should be "date unknown" instead, reverse with:
`update contacts c set cr_accepted_at = null from migration_audit m where m.run_id='f18-2026-09-20' and m.phase='f16_5_cr_accepted_at_backfill' and m.detail->>'transition_from'='Already connected' and m.target_id=c.id;`

Every stamped row is logged in migration_audit (phase `f16_5_cr_accepted_at_backfill`, action `cr_accepted_at_stamped`, before/after, transition_from). Side effect: the backfill UPDATE bumped updated_at on those 36 contacts and wrote 36 ordinary 'updated' audit_log rows (connection_status unchanged in them, so they are not transitions).

### 2. Local Edge Function edit, AWAITING DEPLOY (after 120, which is done)

`supabase/functions/update-contact-on-cr-accepted/index.ts`:

```diff
@@ lines 87 and 94 (both lookups)
-        .from("contacts").select("id, connection_status, sn_lists, company_id")
+        .from("contacts").select("id, connection_status, sn_lists, company_id, cr_accepted_at")
@@ line 123
-    const today = new Date().toISOString().slice(0, 10);
+    // F16.5 (2026-09-20): ... (4 comment lines)
+    const nowIso = new Date().toISOString();
+    const today = nowIso.slice(0, 10);
-      .update({ connection_status: "Accepted", last_contacted: today, updated_at: new Date().toISOString() })
+      .update({ connection_status: "Accepted", cr_accepted_at: contact.cr_accepted_at ?? nowIso, connection_status_source: "phantom_recently_connected", last_contacted: today, updated_at: nowIso })
```

The F15.4 guard (return `already_accepted` for 'Accepted' / 'Already connected' before any write) is untouched, so a repeat event still writes nothing. `contact.cr_accepted_at ?? nowIso` keeps an earlier stamp on accepted -> withdrawn -> accepted. `connection_status_source` has no CHECK constraint; 'phantom_recently_connected' is a new value (existing values are workbook_migration:*, unattributed*, manual_verified_salesnav:*). Because the source now changes in the same update, `fn_stamp_connection_status` will no longer overwrite it with 'unattributed:service_role'.

### 3. Trigger

`trg_stamp_cr_accepted_at` BEFORE UPDATE ON contacts -> `fn_stamp_cr_accepted_at()`: when new status is 'Accepted', old status IS DISTINCT FROM 'Accepted' and new.cr_accepted_at is null, set now(). Never overwrites, never fires on Accepted -> Accepted. `trg_stamp_connection_status` / `fn_stamp_connection_status` untouched (md5 e9cdeb26f3ee3d6b1a099d77079711df). Ordering is irrelevant: checked that no trigger function on contacts assigns connection_status or cr_accepted_at. With the EF edit deployed the EF value wins and the trigger is a no-op; until then the trigger alone stamps new acceptances.

## Files

- `/migrations/119_f16_4_company_archived_reason.sql` (applied)
- `/migrations/120_f16_5_cr_accepted_at.sql` (applied)
- `/supabase/functions/generate-draft-from-context/index.ts` (local edit, not deployed)
- `/supabase/functions/update-contact-on-cr-accepted/index.ts` (local edit, not deployed)
